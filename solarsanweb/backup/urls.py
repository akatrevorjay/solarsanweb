
from django.conf.urls.defaults import patterns, url, include
from django.views import generic

import backup.views
#from backup.models import Pool, Dataset, Snapshot

urlpatterns = patterns('backup.views',
)

